# Freetp.org Telegram Bot

Telegram бот для парсинга и отслеживания игр с сайта freetp.org.

## Функциональность

- 📋 Отображение списка из 10 последних игр с главной страницы
- 🔔 Автоматические уведомления о новых играх
- ⏰ Автономная работа (проверка каждый час)
- 💾 Сохранение состояния между перезапусками

## Установка

1. Клонируйте репозиторий или скачайте файлы
2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

4. Заполните `.env` файл:
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
FREETP_URL=https://freetp.org
CHECK_INTERVAL=0 * * * *
NOTIFICATION_CHAT_ID=
```

### Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

## Запуск

### Разработка
```bash
npm run dev
```

### Продакшн
```bash
npm run build
npm start
```

## Команды бота

- `/start` - Приветственное сообщение
- `/games` - Показать последние 10 игр
- `/newgames` - Проверить новые игры вручную
- `/help` - Справка по командам

## Структура проекта

```
freetp-coop-bot/
├── src/
│   ├── bot.ts          # Основной файл бота
│   ├── parser.ts       # Парсер для freetp.org
│   ├── storage.ts      # Хранение данных о играх
│   └── types.ts        # TypeScript типы
├── data/               # Данные (создается автоматически)
│   └── games.json      # Список известных игр
├── dist/               # Скомпилированный код
├── .env                # Конфигурация (не в git)
├── env.example         # Пример конфигурации
├── package.json
├── tsconfig.json
├── DEPLOY.md           # Гайд по деплою на VPS
└── README.md
```

## Технологии

- **Node.js** + **TypeScript** - основной стек
- **node-telegram-bot-api** - работа с Telegram API
- **cheerio** - парсинг HTML
- **node-cron** - планирование задач
- **axios** - HTTP запросы

## Деплой на VPS

Подробная инструкция по деплою на VPS сервер находится в файле [DEPLOY.md](./DEPLOY.md).
